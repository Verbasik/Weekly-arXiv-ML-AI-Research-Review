# A2A/hosts/multiagent/agent.py
"""
Модуль инициализации корневого агента-хоста.

Создает экземпляр HostAgent, который координирует взаимодействие с удаленными 
агентами на указанных адресах, и инициализирует корневой агент системы.
"""

# Внутренние модули
from .host_agent import HostAgent


# Инициализация корневого агента с подключением к локальному агенту
# на стандартном порту
root_agent = HostAgent(
    remote_agent_addresses=["http://localhost:10000"]
).create_agent()